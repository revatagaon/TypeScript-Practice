function addFunction(a: number, b: number) {
  return a + b;
}

let x = 10;
let y = 13;
const ans = addFunction(x, y)
console.log("The answer is", ans)

// The Syntax is as follow
// var variableName:datatype=value        same goes for let and const
// var name:string="Praveen Revatagaon"